const { client } = require('./client');

async function getAllUsers() {
    try {

    } catch (error) {
        console.error(error);
        throw error;
    }
}


module.exports = {
    getAllUsers,

}